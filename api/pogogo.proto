syntax = "proto3";

package pogogo;

service Pogogo{
    rpc Upload(UploadRequest) returns (UploadResponse) {}
}

message UploadRequest {
    string password = 1;
    bytes  image    = 2;
}

message UploadResponse {
    int32 status = 1;
    string Msg = 2;
}